# 应用同步机制说明

汇总应用数据同步（`app-sync-notify-pull` / `app-sync-changelog-pull`）相关的通知、拉取、增量游标机制，排查同步相关问题时优先查这份文档。内容合并自原 `组织新增通知与拉取流程说明.md`、`增量拉取接口字段说明.md` 两份文档。

## 目录

1. [整体链路（以新增组织为例）](#1-整体链路以新增组织为例)
2. [逐步说明](#2-逐步说明)
3. [`GET /open/api/sync/changes` 接口说明](#3-get-openapisyncchanges-接口说明)
4. [`sinceSeq` 与 `changeSeq` 的边界语义（易错点）](#4-sinceseq-与-changeseq-的边界语义易错点)
5. [关键点](#5-关键点)

## 1. 整体链路（以新增组织为例）

以"新增一个组织"为例，梳理从领域事件产生、写入变更流水、通知候选应用，到应用主动拉取数据的完整链路，附对应代码位置，便于排查问题时定位到具体环节。

```
OrgServiceImpl.create()
  → 写 tab_org（version=1）
  → domainEventPublisher.publish(DomainChangeEvent{ORG, CREATE, ...})
       ↓（事务 afterCommit 后才真正入队，避免读到未提交数据）
DisruptorDomainEventPublisher（Disruptor RingBuffer）
       ↓（唯一消费者线程）
DomainChangeEventHandler → DomainChangeEventProcessor.process(event)
       ↓
  ┌─ DomainChangeRecorder.record(event)  【一个本地事务】
  │    1. AppDataChangeLogService.append(event)   写一条全局变更流水
  │    2. NotifyCandidateResolver.resolveCandidateAppRefIds(event)  判定候选应用
  │    3. 对每个候选应用 AppNotifyTaskService.enqueueTask(...)   落一条 PENDING 通知任务
  └─ 事务提交后 → NotifySendCoordinator.submitImmediateSend(task)（异步，仅低延迟优化）
       ↓
  抢占 PENDING→PROCESSING → AppNotifyService.sendOnce()（HTTP 回调应用）
       ↓成功                                ↓失败
   SUCCESS                          RETRY（指数退避）/ DEAD（达上限）
                                            ↓
                            NotifyRetryScheduler 定时扫描到期任务兜底重试

【应用侧收到通知（只是指针，不含完整数据）】
  → GET /open/api/sync/pull?dataType=ORG&ids=<bizId>   复核详情
    （或 GET /open/api/sync/changes?entityType=ORG&sinceSeq=...  增量游标拉取）
       ↓
  SyncPullServiceImpl.pull() → 组织范围过滤 → 查 tab_org 当前状态 → 组装响应
       ↓
  AppPullRecordService.record(...)   写一条拉取日志（仅问题排查用，失败不影响主流程）
```

## 2. 逐步说明

### 2.1 创建组织，产生领域事件

`OrgServiceImpl.create`（`backend/src/main/java/cn/nihility/rbac/org/service/impl/OrgServiceImpl.java:194`）

写入 `tab_org`（`version` 固定从 1 开始）后，发布一个 `DomainChangeEvent`：
- `dataType=ORG`
- `operationType=CREATE`
- `bizId=` 新组织 id
- `entityVersion=1`
- `orgScopePathBefore=null`
- `orgScopePathAfter=` 新组织的 `orgPath`

### 2.2 事件发布器：等事务提交后才真正入队

`DisruptorDomainEventPublisher.publish`

`create` 方法本身带 `@Transactional`，事件先注册一个 `afterCommit` 回调，业务事务真正提交后才把事件放进 Disruptor 的 RingBuffer——这一步才第一次生成雪花 `eventId`，避免消费者线程用另一个数据库连接读到本次事务里还没提交的数据。

### 2.3 唯一消费者落库

`DomainChangeEventProcessor.process` → `DomainChangeRecorder.record`

在**一个本地事务**里做两件事，任一步失败整体回滚：
- 写一条全局变更流水到 `tab_app_data_change_log`，数据库自增 `change_seq` 回填；
- 判定候选应用（`NotifyCandidateResolver`：该应用 ORG 数据域已启用同步 + 总开关开启 + 同步方式=NOTIFY + 组织范围命中新组织的 `orgPath`），为每个候选应用创建一条 `PENDING` 通知任务，请求体（`NotifyPayload`：`eventId`/`changeSeq`/`entityVersion`/`bizId`/`bizCode`/`occurredAt` 等）在这一步就固化成快照，后续重试不再重新计算。

### 2.4 发送通知

`NotifySendCoordinator`

事务提交后异步提交一次"即时发送"：先原子抢占任务状态 `PENDING→PROCESSING`（带租约），抢占成功才真正发起 HTTP 请求到应用配置的回调地址。成功记 `SUCCESS`；失败按错误类型判断是否可重试，可重试进入 `RETRY`（指数退避），达到最大次数或不可重试类型直接 `DEAD`。即使这次即时发送因进程崩溃没跑起来，任务已经是 `PENDING` 落库状态，独立的 `NotifyRetryScheduler` 定时扫描到期任务能兜底捞到并重发。

### 2.5 应用收到通知后自己去拉数据

通知请求体只是一个"有变更了"的指针（不含完整业务数据），应用拿到 `bizId` 后自己调用：
- `GET /open/api/sync/pull?dataType=ORG&ids=<bizId>` 复核详情（最常见），或
- `GET /open/api/sync/changes?entityType=ORG&sinceSeq=...` 走增量游标续传，拿到指针后再复核。

### 2.6 拉取接口内部处理

`SyncPullServiceImpl.pull`

校验总开关和数据域开关 → 用 `AppSyncOrgScopeResolver.resolveAllowedOrgIds` 算出该应用的组织范围（`id IN (...)`）→ 查 `tab_org` 当前状态并按字段映射组装响应（含 `bizId`/`bizCode`/`bizStatus`/`version` 等固定键）→ 通过 `AppPullRecordService.record` 写一条拉取日志（仅用于管理端问题排查展示，写入失败不影响本次拉取结果）。

## 3. `GET /open/api/sync/changes` 接口说明

对应 OpenSpec change：`openspec/changes/app-sync-changelog-pull/`（已完成实现，尚未归档）。

### 3.1 接口用途

按数据类型（`entityType`）增量拉取应用当前可见范围内的**变更指针**，不返回业务数据本身；只返回"发生了什么变更"的定位信息，业务数据详情需要调用方另行调用 `GET /open/api/sync/pull?ids=<entityId>` 复核。

### 3.2 响应结构

```
SyncChangesPageVO
├── entityType    数据类型：ORG/USER/POSITION/APP/ROLE（不含 DICT）
├── sinceSeq      本次请求实际使用的起始游标（十进制字符串）
├── nextSeq       下一次请求应传入的游标（十进制字符串）
├── hasMore       底层变更流水是否还有未扫描的记录
├── configEpoch   应用级同步配置纪元（十进制字符串）
└── records[]     本页变更指针列表（SyncChangePointerVO）
```

### 3.3 `records[]` 每条变更指针字段

| 字段 | 类型 | 作用 |
| --- | --- | --- |
| `eventId` | 十进制字符串（雪花 ID） | 全局唯一事件标识，用于**幂等追踪**——同一个业务变更若因网络重试/重复投递被看到两次，靠它去重。同一次变更若影响多个应用，这些应用收到的通知共享同一个 `eventId`。**不参与排序**，不保证严格递增，只保证全局唯一。 |
| `entityType` | 字符串 | 数据类型：`ORG`/`USER`/`POSITION`/`APP`/`ROLE`，说明这条变更属于哪类实体。 |
| `entityId` | 十进制字符串 | 被变更的业务实体主键 id，例如具体是哪个组织/用户/角色。 |
| `operationType` | 字符串 | 操作类型：`CREATE`/`UPDATE`/`ENABLE`/`DISABLE`/`DELETE`。 |
| `entityVersion` | 十进制字符串 | 该实体**变更后的结果版本号**（每次写操作原子 `+1`，创建时为 1）。调用方可用它做乱序/重复保护：若本地已缓存版本 ≥ 该值的数据，直接忽略这条指针，不必只靠 `changeSeq` 顺序防线。 |
| `changeSeq` | 十进制字符串 | 数据库自增主键，代表这条流水在全局变更表里的**持久化顺序**，专用于**游标续传**——下次请求把上次响应的 `nextSeq` 作为 `sinceSeq` 传入即可接着往后拉，不重复不遗漏。允许出现空洞（如事务回滚），但严格递增。 |
| `changeTime` | 时间戳 | 变更实际发生的业务时间，仅供展示/审计参考，不用于排序或去重。 |

三者容易被混淆，设计上明确不可互换：

- **`eventId`** → 幂等去重（雪花全局唯一，不保证有序）
- **`changeSeq`** → 游标续传排序（数据库自增，严格递增，允许空洞）
- **`entityVersion`** → 新旧数据判断（同一实体的版本比较，乱序保护）

### 3.4 典型调用流程

1. 调用 `GET /open/api/sync/changes?sinceSeq=<上次的 nextSeq>&entityType=ORG&pageSize=...`。
2. 遍历 `records[]`，对每条指针调用 `GET /open/api/sync/pull?ids=<entityId>` 复核详情：
   - **查得到** → 正常应用这条数据（结合 `entityVersion` 做幂等/乱序判断）。
   - **查不到** → 该实体已"离开范围"或被删除（隐式信号，无单独伪删除操作码），本地应清理对应缓存。
3. 将本次响应的 `nextSeq` 保存下来，作为下一次请求的 `sinceSeq`；`hasMore=true` 时应继续翻页。
4. `sinceSeq` 早于变更流水保留窗口下界时接口返回业务错误，需改走 `GET /open/api/sync/pull` 全量重建，并以 `GET /open/api/sync/digest` 返回的 `currentMaxSeq` 作为新的起始游标衔接增量。

## 4. `sinceSeq` 与 `changeSeq` 的边界语义（易错点）

这是接入方最容易踩的坑：通知体（`NotifyPayload`）里的 `changeSeq` 字段和 `/changes` 接口的请求参数 `sinceSeq`，故意没有共用同一个名字，但语义是配套的——混淆两者会导致"明明收到了通知，却在 `/changes` 里查不到这条记录"。

### 4.1 根因：`sinceSeq` 是不含边界（exclusive）

`/open/api/sync/changes` 底层查询（`AppDataChangeLogMapper.xml`）：

```sql
AND change_seq > #{sinceSeq}
```

严格大于，**不包含 `sinceSeq` 本身这一条**。

如果把通知里拿到的 `changeSeq`（例如 `105`）原样传给 `sinceSeq=105` 去查，服务端只会返回 `change_seq > 105` 的记录——**105 这条自己反而被排除在外**，看起来就像"这条通知的 `changeSeq` 无法获取到"。

### 4.2 两种诉求，两种传法

| 诉求 | `sinceSeq` 应该传什么 |
| --- | --- |
| 想直接复核这条通知本身对应的那条流水 | `changeSeq - 1`（如 `104`），这样 `change_seq > 104` 会把 `105` 也带回来 |
| 处理完这条通知后，接着往后拉后续变更（更常见） | 直接传这条通知的 `changeSeq`（`105`）——这条已经处理过了，下次应该只要 105 **之后**的 |

一句话总结：**通知里的 `changeSeq` 本身不是用来"查它自己"的，而是用来当作"我已经处理到这里了"的水位号**；把它原样存成本地游标、下次原样传给 `sinceSeq` 即可自动跳过已处理的这条，接着拿后续变更，不重复不遗漏。如果非要用 `/changes` 反查这一条具体记录，记得减 1 再传。

### 4.3 自查清单

如果按上述规则传参后仍然查不到期望的记录，按顺序排查：

1. `entityType` 是否传对了（对应通知里的 `dataType`，两者取值一致：`ORG`/`USER`/`POSITION`/`APP`/`ROLE`，但字段名不同，别传错成通知体字段名）；
2. 是不是把 `sinceSeq` 和 `nextSeq` 搞反了（`sinceSeq` 是请求参数，`nextSeq` 是响应里"下次该传的值"，两者数值上通常相等但不要混淆用途）；
3. 组织范围过滤是否把这条记录挡住了（该应用的 ORG/USER/POSITION 数据域组织范围配置是否覆盖了这条记录的组织）；
4. `sinceSeq` 是否早于变更流水保留窗口下界（会返回明确的业务错误，而不是空结果，容易和"查不到"混淆）。

## 5. 关键点

通知只是触发拉取的信号，真正的数据传输方向永远是应用主动 pull——即便"新增组织"这条事件的通知失败、多次重试，只要应用最终调用了 `/pull` 或 `/changes`，依然能拿到这条数据，这也是为什么系统同时提供"通知"和"对账摘要拉取"两条互相兜底的路径。
