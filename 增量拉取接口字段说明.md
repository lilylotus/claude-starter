# `GET /open/api/sync/changes` 变更指针字段说明

对应 OpenSpec change：`openspec/changes/app-sync-changelog-pull/`。

## 接口用途

按数据类型（`entityType`）增量拉取应用当前可见范围内的**变更指针**，不返回业务数据本身；
只返回"发生了什么变更"的定位信息，业务数据详情需要调用方另行调用
`GET /open/api/sync/pull?ids=<entityId>` 复核。

## 响应结构

```
SyncChangesPageVO
├── entityType    数据类型：ORG/USER/POSITION/APP/ROLE（不含 DICT）
├── sinceSeq      本次请求实际使用的起始游标（十进制字符串）
├── nextSeq       下一次请求应传入的游标（十进制字符串）
├── hasMore       底层变更流水是否还有未扫描的记录
├── configEpoch   应用级同步配置纪元（十进制字符串）
└── records[]     本页变更指针列表（SyncChangePointerVO）
```

## `records[]` 每条变更指针字段

| 字段 | 类型 | 作用 |
| --- | --- | --- |
| `eventId` | 十进制字符串（雪花 ID） | 全局唯一事件标识，用于**幂等追踪**——同一个业务变更若因网络重试/重复投递被看到两次，靠它去重。同一次变更若影响多个应用，这些应用收到的通知共享同一个 `eventId`。**不参与排序**，不保证严格递增，只保证全局唯一。 |
| `entityType` | 字符串 | 数据类型：`ORG`/`USER`/`POSITION`/`APP`/`ROLE`，说明这条变更属于哪类实体。 |
| `entityId` | 十进制字符串 | 被变更的业务实体主键 id，例如具体是哪个组织/用户/角色。 |
| `operationType` | 字符串 | 操作类型：`CREATE`/`UPDATE`/`ENABLE`/`DISABLE`/`DELETE`。 |
| `entityVersion` | 十进制字符串 | 该实体**变更后的结果版本号**（每次写操作原子 `+1`，创建时为 1）。调用方可用它做乱序/重复保护：若本地已缓存版本 ≥ 该值的数据，直接忽略这条指针，不必只靠 `changeSeq` 顺序防线。 |
| `changeSeq` | 十进制字符串 | 数据库自增主键，代表这条流水在全局变更表里的**持久化顺序**，专用于**游标续传**——下次请求把上次响应的 `nextSeq` 作为 `sinceSeq` 传入即可接着往后拉，不重复不遗漏。允许出现空洞（如事务回滚），但严格递增。 |
| `changeTime` | 时间戳 | 变更实际发生的业务时间，仅供展示/审计参考，不用于排序或去重。 |

## 三个"类 id"字段的职责边界

三者容易被混淆，设计上明确不可互换：

- **`eventId`** → 幂等去重（雪花全局唯一，不保证有序）
- **`changeSeq`** → 游标续传排序（数据库自增，严格递增，允许空洞）
- **`entityVersion`** → 新旧数据判断（同一实体的版本比较，乱序保护）

## 典型调用流程

1. 调用 `GET /open/api/sync/changes?sinceSeq=<上次的 nextSeq>&entityType=ORG&pageSize=...`。
2. 遍历 `records[]`，对每条指针调用 `GET /open/api/sync/pull?ids=<entityId>` 复核详情：
   - **查得到** → 正常应用这条数据（结合 `entityVersion` 做幂等/乱序判断）。
   - **查不到** → 该实体已"离开范围"或被删除（隐式信号，无单独伪删除操作码），本地应清理对应缓存。
3. 将本次响应的 `nextSeq` 保存下来，作为下一次请求的 `sinceSeq`；`hasMore=true` 时应继续翻页。
4. `sinceSeq` 早于变更流水保留窗口下界时接口返回业务错误，需改走 `GET /open/api/sync/pull` 全量重建，并以
   `GET /open/api/sync/digest` 返回的 `currentMaxSeq` 作为新的起始游标衔接增量。
