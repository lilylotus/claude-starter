# Netty多用户聊天系统与端到端加密设计

## 一、总体架构

客户端通过长连接接入Netty网关层，经TLS加密、编解码、心跳检测后进入业务处理层完成认证鉴权与会话路由，再分别对接集群消息总线做跨节点转发，以及存储与审计层保存离线消息、历史记录与敏感词过滤，集群消息总线也会将其他节点转发来的消息投递回业务处理层。

```
客户端(多用户接入，TCP/WebSocket长连接)
        ↓
Netty网关层（TLS+编解码+心跳检测）
        ↓
业务处理层（认证鉴权+会话路由）
        ↓                          ↓
集群消息总线                  存储与审计
（MQ/Redis跨节点转发）        （离线消息+历史+敏感词）
        ↑ 转发至本地会话（虚线回路）
```

---

## 二、连接与会话管理

- **Channel 与用户身份映射**：连接建立后是"匿名连接"，认证通过才把 `userId` 和 `Channel` 绑定（用 `Channel.attr(AttributeKey)` 存 userId，配合全局 `Map<userId, Channel>` 或 `ChannelGroup`）。断开时在 `channelInactive` 里清理映射，避免内存泄漏。
- **心跳与断线检测**：用 `IdleStateHandler` 检测读写空闲，客户端定时发心跳包，服务端超阈值未收到则主动关闭连接并清理会话；客户端设计指数退避重连，重连后重新认证并补偿拉取离线消息。
- **多端登录**：映射关系设计为 `userId -> Set<Channel>`，消息投递时遍历该用户下所有在线Channel广播。

---

## 三、消息协议设计——处理粘包/半包

TCP是流式协议，必须自定义应用层协议：

```
| 魔数(4B) | 版本(1B) | 消息类型(1B) | 消息长度(4B) | 消息体(变长, Protobuf/JSON) |
```

- 用 Netty 内置的 `LengthFieldBasedFrameDecoder` 按长度字段自动切帧，解决粘包半包，不要自己手写状态机拆包。
- 消息体推荐 **Protobuf**：体积小、序列化快，强schema能天然减少字段篡改和格式错误。
- 消息类型区分：登录/心跳/单聊/群聊/ACK确认/已读回执等，用一个字节的type字段路由到不同Handler。

---

## 四、高并发支撑——线程模型与资源管理

- **EventLoopGroup分工**：`bossGroup`（1个线程即可，只管accept）+ `workerGroup`（线程数=CPU核数×2，负责IO读写）。业务逻辑（查数据库、复杂路由计算）不能放在workerGroup的IO线程里跑，要通过 `pipeline.addLast(businessExecutorGroup, ...)` 提交到独立业务线程池。
- **零拷贝与内存复用**：开启 `PooledByteBufAllocator`（Netty默认已池化），减少频繁GC；大文件/图片走 `FileRegion` 零拷贝传输。
- **背压控制**：设置Channel写缓冲区高低水位线（`WRITE_BUFFER_WATER_MARK`），慢客户端消费不过来时触发 `channelWritabilityChanged`，主动降速或丢弃非核心消息。
- **单机连接数优化**：调大文件描述符上限、TCP `backlog`，聊天这种小包高频场景建议开 `TCP_NODELAY` 减少延迟。

---

## 五、集群与水平扩展

- **无状态网关 + 集中式会话路由**：每台Netty服务器只管理自己机器上的连接，`userId -> 所在网关节点` 的映射存到Redis。
- **跨节点消息转发**：A连节点1，给连节点2的B发消息时，节点1查Redis找到B在节点2，通过消息总线（Kafka/RocketMQ Topic或Redis Pub/Sub）投递给节点2，节点2从本地Channel映射找到B推送。
- 网关无状态化便于水平扩容和故障转移，前面挂负载均衡（LVS/Nginx做WebSocket代理）按连接做粘性分发，业务状态不落在网关本地。

---

## 六、聊天内容安全性

**1. 传输层加密**：全部走TLS（Netty配 `SslContext`，WebSocket用 `wss://`），防止中间人窃听篡改，是最基本的一层。

**2. 身份认证与授权**：建连后要求限定时间内完成登录（JWT token），超时未认证强制断开（也是DoS防护）。Token需有过期和刷新机制，服务端校验签名防伪造。

**3. 消息完整性与防重放**：每条消息带唯一 `msgId`（客户端生成）+ 时间戳，服务端对同一msgId短时间内重复提交做去重，防止重复投递和重放攻击。高安全场景可对消息体做HMAC签名防篡改。

**4. 内容安全（敏感信息/违规内容）**：消息落库和推送前经敏感词过滤（DFA/AC自动机高效匹配），命中后拦截/替换/标记待审核，走异步链路不阻塞主消息路径。图片/文件类消息异步调内容审核接口，先放行发送再异步审核。记录审计日志（谁发的、发给谁、内容摘要/hash），满足合规追溯，日志本身也要做访问权限控制。

**5. 端到端加密场景**（详见下节）：如果要求服务端也无法看到明文，需引入端到端加密方案。

**6. 防滥用**：每个用户/IP限流（令牌桶），防止刷屏/恶意灌包；建立黑名单/风控规则，异常行为触发临时限制。

---

## 七、消息可靠性

- **ACK确认机制**：客户端发消息后服务端回"已收到"确认，超时未收到ACK客户端重发（配合msgId去重，不会重复入库）。
- **离线消息**：用户不在线时消息存入离线消息表/队列，上线后按顺序推送，客户端确认已读后清理。
- **消息顺序**：同一会话内用会话级别的序号保证顺序，而不是单纯依赖到达时间。

---

## 八、端到端加密（E2EE）设计

核心原则：**服务端任何时候都不应该持有能解密消息内容的密钥**，只负责路由密文和分发公钥。

```
用户A客户端（本地生成消息密钥并加密）
        ↓
服务端（仅路由密文，不持有会话密钥）
        ↓
用户B客户端（本地解密还原明文）

服务端 ←→ 密钥分发服务（公钥预共享包）
服务端 → 加密存储（密文直接落库）
```

### 1. 密钥体系——参考Signal协议的X3DH

每个设备本地生成三类密钥对（私钥永远不离开设备）：

- **身份密钥（Identity Key）**：长期密钥，标识设备/账号身份，公钥上传服务端。
- **签名预共享密钥（Signed PreKey）**：中期密钥，定期轮换（如每周），用身份私钥签名防伪造。
- **一次性预共享密钥（One-time PreKey）**：一批一次性公钥，用一个少一个，用完需及时补传。

**首次建立会话（异步握手，对方不在线也能发起）**：

1. A向服务端请求B的预共享密钥包（IdentityKey + SignedPreKey + 一个OneTimePreKey）。
2. A本地用自己私钥和B的公钥做X3DH计算，推导出共享密钥，全程在本地完成，服务端只转发公钥。
3. A用共享密钥加密首条消息，连同用了哪个OneTimePreKey一起发给服务端转发。
4. B上线后用自己私钥做同样DH计算，推导出同一共享密钥解密消息，该OneTimePreKey作废防重放。

### 2. 消息级前向保密——Double Ratchet

固定共享密钥泄露会导致全部历史消息可解，Double Ratchet算法解决：

- 每发一条消息，双方各自对密钥做一次KDF前进一步，生成新消息密钥，用完即弃。
- 叠加DH棘轮：定期交换新的临时DH公钥，刷新根密钥。

效果：
- **前向保密**：泄露今天的密钥，解不出昨天的历史消息。
- **后向安全/自愈**：密钥被攻破后，正常通信几轮会自动愈合，恢复安全。

建议直接用成熟开源库（libsignal-protocol-java/Kotlin），不建议自己实现棘轮算法。

### 3. 群聊场景——Sender Key机制

一对一场景两两建棘轮会话，N人群聊开销随人数平方增长，不现实。群聊推荐Sender Key方案：

1. 每个成员本地生成自己的Sender Key（对称密钥），通过一对一加密通道分发给群内其他成员（O(N)，仅在成员变动/密钥轮换时执行）。
2. 发消息时用自己的Sender Key加密一次，服务端原样转发给所有成员，各自用发送者的Sender Key解密——加密只做一次，不随群人数增长。
3. 成员退群/被踢时必须触发Sender Key轮换（剩余成员重新生成并分发新Key），否则退群者理论上仍可能解密之后截获的密文。

### 4. 内容存储加密——和E2EE要一起考虑

**已做E2EE的场景**：服务端收到的、需要落库的数据本身已经是密文，直接存密文即可，不需要也不能再加密。安全重点转向：

- 服务端存的是"无法解密的密文+元数据（收发人、时间、大小）"，即使拖库拿到的也只是密文；但元数据本身也是隐私，高要求场景需做元数据最小化。
- **多设备同步/云端备份的历史消息**：新设备登录时由已登录的旧设备通过设备间加密通道，把历史消息密钥（或重新加密的历史消息）安全传给新设备，服务端只搬运密文。若用户所有设备丢失且未导出备份密钥，历史消息将无法恢复，这是E2EE设计上必须向用户说明的代价。

**非E2EE场景**（如企业内部合规聊天需服务端审计）：用**信封加密**，每条消息用随机DEK加密，DEK再用主密钥/KMS加密后存储，主密钥托管在KMS（阿里云KMS/HashiCorp Vault），定期轮换。此模式服务端理论上可解密，安全性弱于E2EE，但换来可审计性和内容过滤能力。

### 5. E2EE与内容安全审核的核心矛盾

做了E2EE后，服务端侧的敏感词过滤、图片审核理论上做不了，因为看不到明文。常见折中：

1. **客户端侧过滤**：敏感词库下发到客户端本地拦截，服务端不参与——只能防误发，不能防故意绕过（越狱客户端可绕过）。
2. **举报+事后解密**：不做实时审核，用户举报时由发送方或接收方客户端配合提交明文供审核，牺牲部分隐私换取可追溯性。
3. **不做完全端到端**，只做传输+存储加密，服务端保留解密能力用于必要审计——这是多数企业级/合规场景的现实选择。**真正的端到端加密和强内容监管在设计上是互斥的**，产品形态需先明确定位。

### 6. 落地建议

- 不要自研密码学协议，直接用 **libsignal** 或 Google 的 Tink 库处理密钥管理和加解密原语，自己只负责密钥存储、UI交互和网络传输的胶水代码。
- 客户端私钥用系统级安全存储（iOS Keychain / Android Keystore），不要明文存本地文件。
- 服务端为预共享密钥包设计耗尽告警：OneTimePreKey用完后新会话建立会退化跳过（安全性略降），需监控并推动客户端及时补充。
- SignedPreKey轮换要有服务端强制策略，超期未轮换的客户端提示强制更新。
