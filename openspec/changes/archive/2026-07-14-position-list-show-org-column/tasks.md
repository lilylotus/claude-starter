## 1. 前端：任职记录表格列调整

- [x] 1.1 `PositionManagementView.vue` 中"所属人员"列（`prop="userName"`）表头文案改为"姓名"
- [x] 1.2 在"姓名"列之后新增"组织"列，展示 `orgName`（表头文案精简为"组织"，不是"所属组织"）

## 2. 验证

- [x] 2.1 `npm run build`（`frontend/` 目录）确认类型检查通过
- [x] 2.2 走查代码确认列顺序为：姓名、组织、任职类型、任职地址、任职电话、状态、显示序号、操作
