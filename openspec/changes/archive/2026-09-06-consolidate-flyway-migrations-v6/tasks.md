## 1. 锁定合并来源与终态清单

- [x] 1.1 重新枚举迁移目录，确认合并来源仍为 V1–V14、V18–V20 共 17 个文件且 V15–V17 不存在；若集合或内容已变化，先更新 OpenSpec 文档并重新请求用户确认
- [x] 1.2 保存 17 个来源脚本的文件名、数值执行顺序、SHA-256 和只读副本，确保 legacy 验证不受后续重写/删除影响且不夹带工作区其他改动
- [x] 1.3 建立旧链最终 schema 清单，核对旧 V1 的 46 张表、增量新增的 27 张表以及所有 ALTER 带来的列、类型、默认值、可空性、主键、索引和约束，确认终态共 73 张业务表
- [x] 1.4 建立旧链最终种子清单，按稳定业务键记录敏感词、角色、默认流程、流程绑定、菜单和权限数据，确认权限集合与 `权限资源.txt` 的 145 个唯一编码一致且 SUPER_ADMIN 最终全量授权

## 2. 重写单一 V1 结构

- [x] 2.1 将 V2 的 5 张聊天表和 V4 的 4 张用户角色规则表按依赖顺序并入 V1，逐表核对四个审计字段、索引和约束
- [x] 2.2 将 V7 的 8 张工作流引擎表和 V11 的 10 张生产审批生命周期表按依赖顺序并入 V1，确认 V1 恰好包含 73 条业务表建表语句且不包含 Flowable `ACT_*`/`FLW_*` 表
- [x] 2.3 将 V3、V6、V7–V9 的既有表 ALTER 折叠回对应 CREATE TABLE，直接表达登录方式、管理员自动角色来源、当前审批节点、轨迹目标用户和流程实例关联的最终字段/索引
- [x] 2.4 将 V11、V12、V14、V18、V19 的 ALTER 折叠回对应 CREATE TABLE，直接表达发布生命周期、DSL v2、表单快照、组织来源、幂等结果和会签拒绝策略的最终字段/索引
- [x] 2.5 更新 V1 文件头，准确列出合并来源 V1–V14、V18–V20，声明仅适用于空 schema、兼容 MySQL 5.7/8.0，并禁止已有数据环境通过删除 history 或 repair 强制切换

## 3. 合并最终种子数据

- [x] 3.1 将 V2 的敏感词及聊天/敏感词菜单权限、V5 的批量角色权限、V10/V14/V20 的流程设计权限并入 V1 的最终种子块，保持菜单父子关系和资源类型与旧链一致
- [x] 3.2 将 V7 的默认角色、流程模型、流程定义和节点规则，以及 V13 的 ORG/USER/POSITION/APP 全局业务绑定并入 V1，保持 current_definition_id 和 definition_id 引用语义一致
- [x] 3.3 将 SUPER_ADMIN 全量授权放在全部权限插入之后并移除历史分段补授，静态核对 `tab_permission` 的 145 个唯一编码与 `权限资源.txt` 集合完全一致
- [x] 3.4 静态扫描新 V1，确认不再包含仅用于历史演进的 ALTER/回填/修正语句，不含 CTE、窗口函数、`JSON_TABLE`、厂商专属 upsert 或其他 MySQL 5.7 不兼容语法

## 4. 清理增量迁移文件

- [x] 4.1 删除 V2–V14、V18–V20 共 16 个增量脚本，并验证迁移目录只剩 `V1__init_schema.sql`，不创建 V15–V17 占位文件
- [x] 4.2 将新 V1 与步骤 1 的终态清单逐项复核，确保 27 张新增表、所有后加列/索引及种子数据均已有唯一的最终定义

## 5. 双版本双路径验证

- [x] 5.1 在显式命名且已复核不指向业务库的 MySQL 5.7 隔离空 schema 中分别执行保存的 legacy 17 文件链和新 V1，比较表/列/默认值/索引/约束及按稳定业务键归一化的种子数据，确认均为 73 张表、145 个权限且无语义差异
- [x] 5.2 在显式命名且已复核不指向业务库的 MySQL 8.0 隔离空 schema 中重复双路径执行与比较，确认同一 V1 无语法错误、结构差异或种子差异；环境不可用时保持任务未完成并报告缺口
- [x] 5.3 指向隔离 baseline schema 运行 `./gradlew test --tests "cn.nihility.rbac.RbacApplicationTests"`，验证 `flyway_schema_history` 只记录成功的 V1
- [x] 5.4 在 `backend/` 运行 `./gradlew test` 完成后端全量回归，记录实际测试结果且不修改依赖或业务配置

## 6. OpenSpec 与资源清单收尾

- [x] 6.1 基于实际 SQL diff、表/权限统计和双版本测试结果，更新本 change 的 proposal/design/tasks 实施结果；若终态偏离已确认设计，暂停并重新请求确认
- [x] 6.2 将 delta 同步到 `openspec/specs/backend-common-utilities/spec.md`，使权威 spec 记录 73 张表、145 个权限、MySQL 5.7/8.0 共用基线及空 schema 限制
- [x] 6.3 比较合并前后权限编码集合并确认 `权限资源.txt` 无需修改，因为本 change 只移动既有菜单/按钮种子的脚本位置，不新增、删除或重命名编码
