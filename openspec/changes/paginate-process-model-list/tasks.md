## 1. 后端分页

- [ ] 1.1 增加流程模型分页服务与 GET /api/workflow/process-models/page，校验页码和容量并补全接口文档，复用 PageResult 与数据库分页。
- [ ] 1.2 保持 updateTime、id 倒序，核验查看权限和静态路由匹配，保留原全量查询接口。
- [ ] 1.3 验证多页数据、末页、空数据、稳定排序、参数校验和权限拒绝，补充必要后端回归测试。

## 2. 前端列表

- [x] 2.1 增加分页 API 封装与类型，流程模型列表接入 page/pageSize/total，默认每页 10 条。
- [x] 2.2 添加分页控件，支持 10/20/50/100 条、翻页、容量变化回首页，保护快速翻页请求乱序。
- [x] 2.3 发布/启用/下线后刷新当前页，验证创建进入设计器及绑定页选项兼容；核对权限资源清单。

## 3. 验证及文档

- [ ] 3.1 运行 backend/gradlew.bat test 与 frontend 的 npm run build，记录实际结果。
- [ ] 3.2 验证列表超过 10 条时每次请求只返回当前页、总数准确、空态和操作后刷新正常。
- [ ] 3.3 实现后委托 openspec-doc-sync 按实际 diff/测试同步过程文档并运行 OpenSpec 严格校验。
