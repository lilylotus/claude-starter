## 1. 回归场景与现场核对

- [ ] 1.1 在可访问环境核验 ADD_USER 的当前绑定、发布版本和默认结束节点；不可访问时记录验证边界，使用等价条件模型。
- [ ] 1.2 增加默认分支直接 END 的回归，覆盖女、非女、null、缺失性别；合法空字符串按字段定义补充覆盖，确认旧实现失败。

## 2. 后端终态与业务收尾

- [ ] 2.1 修复流程启动后投影回填与终态同步，保护监听器设置的节点和 TERMINATED，覆盖直接通过、直接拒绝及仍运行情形。
- [ ] 2.2 提交申请关联流程后按实际终态处理，通过复用业务写入、拒绝不写入、运行中保留待审批；完善结果 ID、锁释放、自动处理审计和身份上下文恢复。
- [ ] 2.3 验证自动通过仅执行一次且真实用户落库；验证业务失败整体回滚及重新提交不产生重复记录。
- [ ] 2.4 返回最新申请状态，更新受影响 DTO/API 描述，保持审批参与标志与现有直接生效响应兼容。

## 3. 前端结果反馈

- [ ] 3.1 公共写结果提示依据申请状态展示，通过后刷新业务列表，拒绝提示未生效，保留人工待审批和关闭审批路径。
- [ ] 3.2 检查用户新增页面的女、非女、空性别反馈与刷新行为，以及共用提示的其他主数据页面兼容性。

## 4. 验证与文档同步

- [ ] 4.1 运行有针对性的流程/申请回归和 backend/gradlew.bat test，记录实际通过结果或环境阻碍。
- [ ] 4.2 在 frontend 运行 npm run build，记录类型检查与构建结果。
- [ ] 4.3 实现完成后委托 openspec-doc-sync 根据真实 diff 和测试同步 proposal/design/tasks；校验 OpenSpec 并报告历史悬挂申请尚未恢复。
