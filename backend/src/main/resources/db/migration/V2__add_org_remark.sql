-- ----------------------------------------------------------------------------
-- 组织管理模块 - 新增备注字段（Flyway 迁移版本 V2）
-- ----------------------------------------------------------------------------

-- ALTER TABLE `tab_org` ADD COLUMN `remark` VARCHAR(255) NULL COMMENT '备注' AFTER `show_order`;
