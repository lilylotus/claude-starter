<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 策略目标应用查询：tab_app_access_policy_target_app 关联 tab_app 回填应用名称。
     用 INNER JOIN 是有意为之：应用若已被逻辑删除，对应的目标应用没有展示价值，不返回。 -->
<mapper namespace="cn.nihility.rbac.appaccess.policy.mapper.PolicyTargetAppMapper">

    <select id="selectByPolicyId" resultType="cn.nihility.rbac.appaccess.policy.dto.PolicyTargetAppVO">
        SELECT
        t.app_id AS app_id,
        a.name AS app_name,
        t.update_time AS update_time
        FROM tab_app_access_policy_target_app t
        INNER JOIN tab_app a ON a.id = t.app_id
        WHERE t.policy_id = #{policyId}
        ORDER BY a.show_order DESC, a.id ASC
    </select>

</mapper>
