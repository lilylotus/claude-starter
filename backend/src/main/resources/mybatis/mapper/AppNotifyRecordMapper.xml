<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 通知日志的动态条件分页查询：单表查询，app_ref_id 精确匹配为必填条件，notify_status
     精确匹配、create_time 时间范围过滤均可选，按通知发起时间（create_time）降序排列
     （add-app-sync-notify-pull-logs change design.md Decision 3）。 -->
<mapper namespace="cn.nihility.rbac.sync.notify.mapper.AppNotifyRecordMapper">

    <select id="selectNotifyRecordPage" resultType="cn.nihility.rbac.sync.notify.entity.AppNotifyRecordEntity">
        SELECT *
        FROM tab_app_notify_record
        <where>
            AND app_ref_id = #{query.appRefId}
            <if test="query.notifyStatus != null">
                AND notify_status = #{query.notifyStatus}
            </if>
            <if test="query.startTime != null">
                AND create_time &gt;= #{query.startTime}
            </if>
            <if test="query.endTime != null">
                AND create_time &lt;= #{query.endTime}
            </if>
        </where>
        ORDER BY create_time DESC, id DESC
    </select>

    <!-- 状态机到期任务扫描（app-sync-changelog-pull change design.md Decision 6）：
         PENDING/RETRY 状态且到期，或 PROCESSING 状态且租约已超时，按 id 升序取前 limit 条，
         具体的原子抢占（UPDATE ... WHERE task_status = 期望值）由 AppNotifyTaskServiceImpl
         另行执行，本查询本身不加锁、不修改数据。 -->
    <select id="selectDueTasks" resultType="cn.nihility.rbac.sync.notify.entity.AppNotifyRecordEntity">
        SELECT *
        FROM tab_app_notify_record
        WHERE (task_status IN ('PENDING', 'RETRY') AND (next_retry_time IS NULL OR next_retry_time &lt;= #{now}))
           OR (task_status = 'PROCESSING' AND lease_until IS NOT NULL AND lease_until &lt; #{now})
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

</mapper>
